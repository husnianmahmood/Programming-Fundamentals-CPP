#include <iostream>
#include <iomanip>
using namespace std;

double celsiusToFahrenheit(double c) {
    return (c * 9 / 5) + 32;
}

int main() {
    double c;
    cin >> c;
    cout << fixed << setprecision(1);
    cout << celsiusToFahrenheit(c) << endl;
    return 0;
}
